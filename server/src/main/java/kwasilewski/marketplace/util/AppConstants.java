package kwasilewski.marketplace.util;

import java.util.concurrent.TimeUnit;

public class AppConstants {

    public static int MAX_PHOTOS = 10;
    public static long DAYS_AFTER_REFRESH_POSSIBLE = TimeUnit.DAYS.toMillis(14);

}
